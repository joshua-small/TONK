# ─── General ────────────────────────────────────────────────
* text=auto

# ─── Line Endings ───────────────────────────────────────────
*.sh     text eol=lf
*.bat    text eol=crlf
*.md     text eol=lf
*.yaml   text eol=lf
*.yml    text eol=lf
*.json   text eol=lf
*.txt    text eol=lf

# ─── Code and Config Files ─────────────────────────────────
*.c      text diff=cpp
*.cpp    text diff=cpp
*.h      text diff=cpp
*.ino    text
*.js     text
*.ts     text
*.py     text

# ─── KiCad ──────────────────────────────────────────────────
*.kicad_sch text
*.kicad_pcb text
*.kicad_sym text
*.kicad_mod text
*.kicad_prl text
*.kicad_pro text
*.kicad_dru text
*.net      text
*.dcm      text

# ─── Vector Art / UI Design ────────────────────────────────
*.svg     text
*.dxf     text

# ─── Binary Files (no diff) ────────────────────────────────
*.pdf     binary
*.step    binary
*.stp     binary
*.iges    binary
*.stl     binary
*.png     binary
*.jpg     binary
*.jpeg    binary

# ─── Git LFS (optional) ─────────────────────────────────────
# If you use Git LFS, uncomment below and run `git lfs track "*.step"`
# *.step filter=lfs diff=lfs merge=lfs -text
# *.stl  filter=lfs diff=lfs merge=lfs -text
# *.pdf  filter=lfs diff=lfs merge=lfs -text

# ─── Ignore merge conflicts for lock or cache files ────────
*.lock    merge=ours
*.cache   merge=ours
